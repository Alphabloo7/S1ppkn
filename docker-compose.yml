services:
  web:
    image: alphabloo7/s1ppkn:latest
    container_name: s1ppkn-web
    restart: always
    ports:
      - "8086:80"
    healthcheck:
      test: ["CMD", "curl", "-fsS", "http://localhost/healthz"]
      interval: 30s
      timeout: 5s
      retries: 3
      start_period: 5s
